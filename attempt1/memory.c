/**
The memory manager. Should use bit hacking to perform allocation, deallocation, and bookkeeping for speed

size_t allocations;

Structure* str = evil_alloc(sizeof(Structure));
evil_free(str);

*/



